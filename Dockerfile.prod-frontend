# 生产环境前端 Dockerfile - 只 COPY 预编译静态文件，无需 Node 环境
FROM nginx:alpine

COPY shop-pc/dist/ /usr/share/nginx/html/
COPY shop-pc/nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
