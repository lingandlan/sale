# 太积堂充值与门店管理系统 - PRD

**文档版本：** v1.3
**创建日期：** 2026-04-07
**最后更新：** 2026-04-08
**作者：** 产品团队
**评审人：**
**状态：** 草稿

---

## 文档版本历史

| 版本 | 日期 | 变更内容 | 作者 |
|------|------|----------|------|
| v1.0 | 2026-04-07 | 初始版本 | 产品团队 |
| v1.1 | 2026-04-08 | 新增异常场景处理规范（Q1-Q6）<br>- B端充值审批拒绝处理<br>- 余额更新失败补偿机制<br>- 充值中心冻结期间规则<br>- C端充值余额不足拦截<br>- 商城接口失败异步重试<br>- 商城系统维护开关 | 产品团队 |
| v1.2 | 2026-04-08 | 新增业务规则章节<br>- 积分比例规则（>=10万按2%，<10万按1%）<br>- 门店卡发放规则（购买2张，推荐1张/人无上限）<br>- 余额计算与对账规则（月初对账）<br>- 核销与售后规则（1年有效期，不退费不补办）<br>- 完善异常场景处理（场景3-5，Q7-Q14） | 产品团队 |
| v1.3 | 2026-04-08 | 完善功能需求章节<br>- 补充C端充值录入的余额校验、失败重试、积分通知机制<br>- 补充门店卡发放的购买/推荐奖励差异处理<br>- 补充门店卡核销的过期检查和核销失败处理<br>- 补充C端小程序的会员门店卡领取页面<br>- 完善外部系统接口（推荐奖励查询、门店卡发放通知、重试机制）<br>- 完善异常场景处理（场景6-8，Q15-Q18）<br>- 修正文档冲突点（月末→月初对账、小程序登录方式） | 产品团队 |

---

## 目录

- [一、背景与目标](#一背景与目标)
- [二、用户场景](#二用户场景)
- [三、业务规则](#三业务规则) ⭐ v1.2新增
- [四、功能需求](#四功能需求)
- [五、外部系统接口](#五外部系统接口)
- [六、非功能需求](#六非功能需求)
- [七、异常场景处理](#七异常场景处理) ⭐ v1.1新增
- [八、风险评估](#八风险评估)
- [九、项目计划](#九项目计划)
- [十、附录](#十附录)

---

## 一、背景与目标

### 1.1 背景

太积堂业务包含两大板块：

**商城系统（已有，独立运行）：**
- 线上购物、会员体系、佣金计算（线下结算）
- 成组成团、积分兑换、会员升级等

**充值与门店管理系统（本系统，待建设）：**
- 充值中心管理、充值记录（含审批流）
- 门店卡发行核销、会员管理、操作日志
- C端小程序（用户查看卡信息）

两个系统通过 `user_id` 数据互通——一个人可能同时是商城会员和门店会员，通过 `user_id` 识别同一用户。

**当前痛点：**
- 充值资金流向不透明，总公司难以掌握各充值中心的收款情况
- 充值操作无记录，出现纠纷时无法追溯责任人
- 门店会员卡（面值1000元/张）缺乏系统化管理，发放和核销无记录
- 财务对账依靠手工台账，效率低、易出错

**注：** 佣金全部在线下结算，本系统不涉及佣金计算功能。

### 1.2 目标

- **账务可追溯：** 每笔充值都有完整操作日志，永久可查
- **门店卡可管控：** 卡发行、归属、核销全程记录，防止丢失和滥用
- **操作可审计：** 日志哈希链机制防篡改，完整记录操作人、时间、IP和数据变更
- **数据互通：** 通过 `user_id` 与商城系统对接，实现会员身份统一识别

### 1.3 成功指标

| 指标 | 目标值 |
|------|--------|
| 充值记录操作日志覆盖率 | 100% |
| 门店卡发行记录覆盖率 | 100% |
| 操作日志查询响应时间 | ≤2秒（P99） |
| 系统可用性 | ≥99.5% |

---

## 二、用户场景

### 2.1 目标用户

| 角色 | 说明 | 操作范围 |
|------|------|----------|
| **超管** | 总公司系统管理员 | 系统配置、充值中心开通、权限管理、所有操作查看 |
| **总部管理员** | 专门负责审批 | B端充值审批、查看所有充值记录和操作日志 |
| **总公司财务运营** | 总公司财务人员 | 发起B端充值申请、查看所有记录、导出对账 |
| **充值中心管理员** | 各充值中心财务负责人 | 管理本中心操作员、C端充值录入、门店卡发放核销 |
| **充值中心操作员** | 各充值中心一线操作人员 | C端充值录入、门店卡发放核销 |
| **持卡消费者** | C端小程序用户 | 查看持有的门店卡和消费记录 |

### 2.2 用户故事

**故事1（总公司财务运营）：** 作为总公司财务运营，我希望在系统中为各充值中心发起充值申请，这样总公司可以清晰地掌握每笔资金的流向和审批状态。

**故事2（总部管理员）：** 作为总部管理员，我希望审批各充值中心的充值申请，根据上月消耗金额确定积分比例，这样充值中心可以合规地增加积分余额。

**故事3（充值中心操作员）：** 作为充值中心操作员，我希望直接为商城会员录入积分充值（扣本店余额），这样会员可以快速获得积分，服务体验更好。

**故事4（充值中心管理员）：** 作为充值中心管理员，我希望管理本中心的门店卡——包括入库，向符合资格的顾客发放门店卡，核销消费——这样我可以完整追踪每张卡的状态。

**故事5（超管）：** 作为超管，我希望查看完整的操作日志，这样出现充值纠纷时可以追溯到具体的操作人和操作时间。

**故事6（持卡消费者）：** 作为持卡消费者，我希望在微信小程序里查看我持有的所有门店会员卡和消费记录，这样我清楚地知道每张卡的余额。

### 2.3 使用场景

**场景A：B端充值（总公司→充值中心，含审批）**

- **触发条件：** 总公司收到充值中心的打款
- **用户行为：** 总公司财务运营选择目标充值中心，录入充值金额，提交申请 → 总部管理员审批（查询上月消耗金额，确认积分比例）→ 通过后充值中心积分余额增加
- **预期结果：** 充值中心积分余额增加，操作日志记录完整
- **积分比例规则：** >=10万 → 充值金额×2%，<10万 → 充值金额×1%

**场景B：C端充值（充值中心→商城会员，直接扣余额，无需审批）**

- **触发条件：** 充值中心收到会员现金（用于充值积分）
- **用户行为：** 充值中心操作员录入会员手机号/user_id和充值积分金额，提交
- **预期结果：** 充值中心积分余额扣减，商城会员积分余额增加，操作日志记录，系统通知商城接口

**场景C：门店卡开店发放（超管操作）**

- **触发条件：** 新店开业，公司给门店划拨初始卡库存
- **用户行为：** 超管选择目标充值中心，选择卡号段，确认发放
- **预期结果：** 卡从公司总卡库划入充值中心库存，状态仍为"已入库"，记录操作日志

**场景D：门店卡给用户发放（充值中心管理员/操作员）**

- **触发条件：** 顾客购买2万元套餐包（或推荐他人购买）
- **用户行为：** 充值中心操作员查询用户消费记录（判断是否满足条件）→ 确认发放原因和数量 → 发放门店卡
- **预期结果：** 卡绑定到用户，卡状态变为"已发放"，记录发放原因

**场景E：门店卡消费核销**

- **触发条件：** 持卡人到门店消费
- **用户行为：** 充值中心操作员输入卡号和消费金额，核销
- **预期结果：** 卡余额扣减，记录核销日志

**场景F：月初对账**

- **触发条件：** 月初财务对账
- **用户行为：** 总公司财务运营选择日期范围，点击导出
- **预期结果：** 生成Excel，含所有B端充值记录、C端充值记录和门店卡记录

**场景G：充值纠纷追溯**

- **触发条件：** 出现充值金额错误等纠纷
- **用户行为：** 总部管理员输入操作人或时间范围查询日志
- **预期结果：** 展示完整的操作轨迹，含操作人、IP、变更前后数据

---

## 三、业务规则 ⭐ v1.2新增

### 3.1 积分比例规则

B端充值审批时，根据充值中心上月商城消费金额确定积分增加比例。

| 项目 | 规则 |
|------|------|
| **消耗定义** | 商城净消费金额 = 成功交易金额 - 退款金额 |
| **比例规则** | >=10万元按2%，<10万元按1% |
| **查询时机** | 每月初定时任务统一计算并缓存，审批时读缓存 |
| **首月处理** | 无消耗记录默认按1% |
| **持续无消耗** | 连续3个月无消耗记录，  - 第2-3个月：仍按1%，但系统发送提醒通知财务运营 |
  - 第4个月起：需总部管理员审批，填写原因并确认后可继续充值（暂停自动审批） |
| **接口失败** | 使用上月缓存数据，并发送告警通知技术团队 |
| **统计周期** | 自然月（1日至月末最后一天） |
| **B端充值精度** | 0.01元（两位小数），以财务到账金额为准 |
| **C端充值精度** | 最低1000积分，必须是1000的倍数 |
| **积分计算** | 向上取整（12,345.67元×1% = 124积分） |
| **缓存策略** | 每月单独存储，不覆盖历史数据 |

**计算示例：**

| 充值中心 | 上月商城净消费 | B端充值金额 | 积分比例 | 增加积分 |
|---------|---------------|------------|---------|---------|
| 北京中心 | 80,000元 | 50,000.00元 | 1% | 500积分 |
| 上海中心 | 150,000元 | 50,000.00元 | 2% | 1,000积分 |
| 广州中心 | 无数据（首月） | 12,345.67元 | 1%（默认） | 124积分 |

---

### 3.2 门店卡发放规则

门店卡发放需满足特定条件，发放数量由商城接口计算返回。

| 场景 | 发放数量 | 规则说明 |
|------|---------|---------|
| **购买套餐包** | 2张 | 顾客购买2万元套餐包，可发放2张门店卡 |
| **推荐奖励** | 1张/人，无上限 | 每成功推荐1人购买套餐包，推荐人可发放1张卡，不限制数量 |
| **数据来源** | 商城接口 | 可发放数量由商城接口根据用户消费记录计算并返回 |
| **发放时限** | 无时限 | 符合条件后永久有效，随时可发放 |

**卡片发放方式：**

| 发放方式 | 适用场景 | 操作流程 | 卡来源 | 绑定方式 |
|---------|---------|---------|--------|---------|
| **实体卡发放** | 购买套餐包 / 推荐奖励 | 运营人员线下发卡 → 后台绑定卡号和用户 | 充值中心库存 / 公司总卡库 | 运营人员在后台输入卡号+被推荐人手机号 |
| **虚拟卡发放** | 购买套餐包 / 推荐奖励 | 用户在小程序自助领取 | 充值中心库存 / 公司总卡库 | 系统自动绑定到用户 |
| **卡片关系** | - | 虚拟卡是实体卡的数字化形式，用户可在小程序查看 | - | - |

**购买套餐包发放：**
- 购买成功后可立即发放
- 实体卡：运营人员在门店现场发放，后台绑定卡号和用户
- 虚拟卡：用户在小程序"我的卡券"中自助领取

**推荐奖励发放：**
- **退款周期：** 7天
- **发放方式：** 7天后可领取（实体卡或虚拟卡）
- **实体卡领取：** 用户到门店找运营人员，运营人员后台绑定卡号和用户
- **虚拟卡领取：** 在小程序中"会员门店卡领取"页面自助领取
- **推荐人资格：** 推荐人必须是会员（持卡消费者）
- **后端验证：** ①被推荐人最近7天购买数 > 退款数<br>②被推荐人与当前推荐人是绑定关系
- **退款处理：** 被推荐人7天内退款，推荐奖励取消
- **可领取资格：** 永久有效，随时可领取
- **领取后有效期：** 从激活日（首次核销）开始计算1年有效期

**商城接口返回示例：**

```json
{
  "user_id": 370955981,
  "has_purchased_package": true,
  "purchasable_card_count": 2,
  "referral_count": 5,
  "referral_card_count": 5,
  "total_available": 7
}
```

---

### 3.3 余额计算与对账规则

#### 3.3.1 余额计算

| 项目 | 规则 |
|------|------|
| **余额存储** | `recharge_center.balance` 字段，实时更新 |
| **余额增加** | B端充值审批通过后增加 |
| **余额扣减** | C端充值录入时扣减 |
| **计算公式** | 当前余额 = balance 字段实时值 |
| **余额精度** | 整数（精确到元） |
| **负数余额** | 不允许，余额不足时禁止操作 |
| **手动调整** | 超管和总部管理员可调整，详细记录操作日志 |

#### 3.3.2 月初对账

**对账周期：** 每月初进行一次完整对账（定时任务每月1号凌晨2:00执行，或总部管理员手动触发）

**对账范围：** 对账上月数据

**对账阈值：** 差异≥1元即告警

**对账流程：**

```
1. 数据采集
   ├─ 本系统：B端充值记录、C端充值记录、充值中心余额快照
   └─ 商城系统：月初/月末余额、本月积分变动明细

2. 数据比对
   ├─ 本系统B端充值总额 = 商城本月积分增加总额？
   ├─ 本系统C端充值总额 = 商城本月会员积分增加总额？
   └─ 期末余额一致性检查

3. 生成对账报表
   ├─ 按充值中心展示差异
   ├─ 差异明细（支持导出Excel）

4. 差异处理（人工）
   ├─ 查看差异明细
   ├─ 定位原因（同步失败/时间差/数据丢失）
   ├─ 差异分级：
   │  └─ 大额差异（≥100元）：1个工作日内处理
   ├─ 处理操作：标记已处理/手动调整/联系商城/生成工单
   ├─ 升级机制：
   │  └─ 记录升级日志和审批轨迹，通过后台手动修正。
   └─ 所有差异处理完成后，对账状态更新为"已对账"
```

**数据表设计：**

```sql
-- 对账记录表
CREATE TABLE reconciliation_log (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    reconciliation_month VARCHAR(7) NOT NULL COMMENT '对账月份：2026-03',
    reconciliation_time DATETIME NOT NULL COMMENT '对账时间',
    reconciliation_type TINYINT DEFAULT 1 COMMENT '对账类型:1=自动,2=手动',
    status TINYINT DEFAULT 1 COMMENT '状态:1=有差异,2=无差异,3=已对账',
    total_centers INT COMMENT '涉及充值中心数量',
    diff_centers INT COMMENT '有差异的充值中心数量',
    operator_id BIGINT COMMENT '手动触发时的操作人ID',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 对账差异明细表
CREATE TABLE reconciliation_diff (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    reconciliation_id BIGINT NOT NULL COMMENT '对账记录ID',
    recharge_center_id BIGINT NOT NULL COMMENT '充值中心ID',
    diff_type TINYINT NOT NULL COMMENT '差异类型:1=B端充值差异,2=C端充值差异,3=余额差异',
    local_amount DECIMAL(20,2) COMMENT '本系统金额',
    mall_amount DECIMAL(20,2) COMMENT '商城金额',
    diff_amount DECIMAL(20,2) COMMENT '差额',
    status TINYINT DEFAULT 1 COMMENT '状态:1=待处理,2=处理中,3=已处理',
    handler_id BIGINT COMMENT '处理人ID',
    handle_result VARCHAR(500) COMMENT '处理结果',
    handle_time DATETIME COMMENT '处理时间',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

---

### 3.4 核销与售后规则

| 场景 | 规则 | 说明 |
|------|------|------|
| **部分核销** | ✅ 允许 | 消费金额可小于卡余额，剩余余额继续有效 |
| **最低消费** | 100元 | 单次核销最低金额100元 |
| **余额精度** | 整数 | 门店卡余额精确到元，不支持小数 |
| **核销次数** | 无限制 | 同一张卡每天核销次数无限制 |
| **身份验证** | 无需验证 | 见卡即付，谁持有卡即可消费 |
| **核销验证** | 无需密码或签名 | 直接核销，无需额外验证 |
| **激活机制** | 首次核销激活 | 门店卡首次核销时自动激活，有效期从激活日期开始计算 |
| **激活时限** | 无时限 | 门店卡发放后永久有效，可随时首次核销激活 |
| **休眠规则** | 无休眠规则 | 长期未消费的卡不会休眠 |
| **退费规则** | ❌ 不允许 | 核销后不允许退费 |
| **挂失补卡** | ❌ 不补办 | 门店卡丢失不补办，余额作废 |
| **有效期** | 🕐 1年 | 从激活日期（首次核销日期）开始计算，有效期1年 |
| **卡面值** | 固定1000元 | 每张卡面值固定1000元，不可调整 |
| **卡转让** | 见卡即付 | 无需转让流程，谁持有卡即可消费 |
| **卡合并** | ❌ 不允许 | 多张卡不能合并，每张卡独立使用 |

**充值中心余额精度：** 整数（精确到元）

**过期处理：**
- 核销时实时检查卡片是否过期
- 过期卡不可核销
- 过期卡余额不可退费

**门店卡特殊场景处理：**

| 场景 | 处理方式 | 说明 |
|------|---------|------|
| **卡损坏无法读取** | 搜索卡号手动核销 | 门店卡本质是数字，可根据发卡记录搜索卡号进行核销 |
| **卡面值错误** | 作废处理 | 面值错误的卡作废，不可调整 |

**数据保留策略：**
- 操作日志、充值记录等在线保留2个月
- 2个月后的数据归档存储（后续使用ES）
- 归档数据可查询，不支持修改

**小程序登录：**
- 采用微信授权获取手机号
- 登录后通过手机号匹配系统账户，根据角色展示对应页面
- 多角色登录流程详细设计待完善（TODO #3）

**相关定时任务：**

| 定时任务 | 执行时间 | 功能 |
|---------|---------|------|
| 积分比例计算 | 每月1号凌晨2:00 | 调用商城接口获取上月消费，计算各充值中心积分比例并缓存 |
| 月初对账 | 每月1号凌晨3:00 | 采集本系统和商城数据，生成对账报表 |

---

### 3.5 充值中心冻结规则

| 项目 | 规则 | 说明 |
|------|------|------|
| **冻结权限** | 超管、总部管理员、充值中心管理员（限本中心） | 充值中心管理员只能冻结本中心 |
| **解冻权限** | 超管、总部管理员 | 充值中心管理员无权解冻 |
| **冻结后限制** | 禁止C端充值、禁止门店卡核销 | B端充值审批通过后自动解冻 |
| **冻结后可见性** | 冻结后仍可查看历史数据 | 不影响数据查询 |

**冻结操作流程：**
1. 有权限用户选择充值中心
2. 填写冻结原因（必填）
3. 确认冻结
4. 系统记录操作日志

**解冻操作流程：**
1. 超管/总部管理员选择已冻结的充值中心
2. 填写解冻原因（必填）
3. 确认解冻
4. 系统记录操作日志

---

### 3.6 门店卡状态流转

| 状态 | 说明 | 可执行操作 |
|------|------|-----------|
| **已入库** | 初始状态，卡在门店库存中 | 可发放给用户 |
| **已发放** | 发放给用户，尚未激活 | 可核销（首次核销自动激活） |
| **已激活** | 首次核销后激活，可正常消费 | 可继续核销 |
| **已冻结** | 异常冻结，不可核销 | 需超管/总部管理员解冻 |
| **已过期** | 有效期到期，不可核销 | 不可恢复 |
| **已作废** | 损坏或其他原因作废 | 不可恢复 |

**状态流转图：**

```
已入库 → 已发放 → 已激活 → 1年 → 已过期
                    ↓
                 已冻结 → 解冻 → 已激活
                    ↓
                 已作废
```

| **状态变更权限：**

| 操作 | 需要权限 | 说明 |
|------|---------|------|
| 已入库 → 已发放 | 充值中心管理员/操作员 | 发放给用户 |
| 已发放 → 已激活 | 系统自动 | 首次核销时自动激活 |
| 已激活 → 已冻结 | 超管/总部管理员/中心管理员 | 冻结卡片 |
| 已冻结 → 已激活 | 超管/总部管理员 | 解冻卡片 |
| 任意 → 已作废 | 超管/总部管理员 | 仅手动作废，无自动作废场景 |

---

### 3.7 操作权限边界

| 角色 | 冻结/解冻中心 | B端充值 | C端充值 | 门店卡发放 | 门店卡核销 | 查看数据 |
|------|-------------|--------|--------|-----------|-----------|---------|
| **超管** | ✅冻结/解冻全部 | ✅审批 | ✅操作 | ✅操作 | ✅操作 | ✅查看全部 |
| **总部管理员** | ✅冻结/解冻全部 | ✅审批 | - | - | - | ✅查看全部 |
| **总公司财务运营** | ❌ | ✅申请 | - | - | - | ✅查看全部 |
| **充值中心管理员** | ✅冻结本中心<br>❌解冻 | - | ✅ | ✅ | ✅ | ✅查看本中心 |
| **充值中心操作员** | ❌ | - | ✅ | ✅ | ✅ | ✅查看本中心 |

**权限说明：**
- ✅ 表示有权限
- ❌ 表示无权限
- - 表示不涉及

**冻结后操作限制：**
| 操作 | 冻结后是否允许 |
|------|--------------|
| C端充值 | ❌ 禁止 |
| 门店卡核销 | ❌ 禁止 |
| B端充值审批 | ✅ 允许（通过后自动解冻） |
| 数据查询 | ✅ 允许 |

---

### 3.8 操作员管理规则

| 项目 | 规则 | 说明 |
|------|------|------|
| **冻结权限** | 超管可冻结所有操作员<br>充值中心管理员可冻结本中心操作员 | 充值中心管理员只能冻结本中心操作员 |
| **冻结后影响** | 冻结后操作员无法登录系统 | 历史操作记录保留 |
| **离职处理** | 冻结账号，历史记录保留 | 不删除账号，保留操作历史可追溯 |
| **创建权限** | 充值中心管理员可创建本中心操作员 | 由充值中心管理员管理 |

---

## 四、功能需求

### 4.1 功能清单

#### 4.1.1 充值中心管理

| 功能点 | 优先级 | 说明 |
|--------|--------|------|
| 充值中心增删改查 | P0 | 含省/市/区/位置/级别 |
| 充值中心冻结/解冻 | P0 | 冻结后不可充值 |
| 充值中心与门店1:1绑定 | P0 | 展示上区分子公司合伙人/服务中心合伙人 |

#### 4.1.2 门店管理

| 功能点 | 优先级 | 说明 |
|--------|--------|------|
| 门店信息维护 | P0 | 由充值中心管理员管理 |

#### 4.1.3 B端充值管理

| 功能点 | 优先级 | 说明 |
|--------|--------|------|
| B端充值申请发起 | P0 | 总公司财务运营发起申请 |
| B端充值审批（审核/确认） | P0 | 总部管理员操作，按比例增加积分余额 |
| B端充值记录查看 | P0 | 按充值中心/时间筛选 |

#### 4.1.4 C端充值管理

| 功能点 | 优先级 | 说明 |
|--------|--------|------|
| C端充值录入（直接扣余额，无需审批） | P0 | 充值中心→会员充积分，余额不足拦截 |
| C端充值记录查看 | P0 | 全部记录可查 |

#### 4.1.5 门店卡管理

| 功能点 | 优先级 | 说明 |
|--------|--------|------|
| 门店卡总库管理（公司级） | P0 | 超管操作，含卡号段批量入库、库存查询、库存告警 |
| 门店卡充值中心库存管理 | P0 | 充值中心管理员操作，查询本中心库存 |
| 门店卡开店发放（总库→充值中心） | P0 | 超管操作，从公司总卡库划拨到充值中心库存 |
| 运营人员绑定卡号和用户 | P0 | 实体卡发放后，运营人员在后台输入卡号+用户手机号进行绑定 |
| 查询用户消费记录 | P0 | 调用商城接口，判断发放条件 |
| 门店卡发放给用户（虚拟卡） | P0 | 用户在小程序自助领取，系统自动绑定 |
| 门店卡消费核销 | P0 | 扣减卡余额，记录核销日志 |
| 门店卡查询/状态管理 | P0 | 按卡号/用户/状态查询 |

#### 4.1.6 C端小程序

| 功能点 | 优先级 | 说明 |
|--------|--------|------|
| 门店卡列表查看 | P0 | 用户查看持有的卡 |
| 门店卡详情（消费记录） | P0 | 查看单张卡消费明细 |
| 门店信息展示 | P0 | 展示门店名称/地址/电话 |
| 会员门店卡领取 | P0 | 推荐奖励领取，列出可领取的门店卡及来源被推荐人 |

#### 4.1.7 操作员账号管理

| 功能点 | 优先级 | 说明 |
|--------|--------|------|
| 操作员账号创建/编辑/冻结 | P0 | 由充值中心管理员管理 |
| 操作员密码重置 | P0 | |

#### 4.1.8 操作日志

| 功能点 | 优先级 | 说明 |
|--------|--------|------|
| 操作日志记录（全操作覆盖） | P0 | 含IP/设备/数据快照 |
| 操作日志查询 | P0 | 按时间/操作人/类型/机构多维筛选 |
| 日志哈希链防篡改 | P0 | |

#### 4.1.9 充值记录导出

| 功能点 | 优先级 | 说明 |
|--------|--------|------|
| Excel导出 | P0 | B端/C端/门店卡分开导出 |
| 多维筛选 | P0 | 按类型/中心/时间/操作人 |

#### 4.1.10 充值中心统计

| 功能点 | 优先级 | 说明 |
|--------|--------|------|
| 首页数据概览 | P1 | 各中心余额/充值统计/卡统计 |

#### 4.1.11 外部系统对接

| 功能点 | 优先级 | 说明 |
|--------|--------|------|
| 接收商城资产变更回调 | P0 | 被动接收 |
| 向商城推送C端充值通知 | P0 | 通知商城给会员加积分 |
| 向商城查询用户消费记录 | P0 | 用于判断卡发放条件+计算返点 |

#### 4.1.12 登录认证

| 功能点 | 优先级 | 说明 |
|--------|--------|------|
| 手机号+密码登录 | P0 | |
| 短信验证码登录 | P0 | |

---

### 4.2 功能详情

#### 4.2.1 充值中心管理

**需求描述：** 超管可对充值中心进行增删改查操作。每个充值中心1:1绑定一个门店，逻辑层统一用"充值中心"，展示层按级别显示不同名称。

**页面元素：**
- 充值中心名称
- 所在省份（必填）
- 所在城市（联动省份，必填）
- 所在区县（必填）
- 具体位置（必填）
- 级别（子公司合伙人/服务中心合伙人，纯信息）
- 管理员（选已有操作员账号）
- 绑定门店名称（自动带入）
- 积分余额（系统计算=累计打入-已消耗）
- 累计充值积分（系统计算）
- 已消耗积分（系统计算）
- 状态（正常/冻结）

**展示名称示例：**
- 子公司合伙人 → `[地级市名]子公司合伙人 - [充值中心名]`
- 服务中心合伙人 → `[县市名]服务中心合伙人 - [充值中心名]`

---

#### 4.2.2 门店管理

**需求描述：** 充值中心管理员可管理本中心的门店信息。门店1:1绑定充值中心。

**页面元素：**
- 门店名称（与充值中心同名，自动带入）
- 所属充值中心（只读）
- 门店地址（与充值中心同步）
- 负责人（选填）
- 联系电话（选填）
- 卡库存数量（系统计算）
- 卡库存余额（系统计算）
- 状态

---

#### 4.2.3 B端充值申请发起（总公司财务运营）

**需求描述：** 总公司财务运营为各充值中心发起充值申请，经过审批后充值中心积分余额才增加。

**页面元素：**
- 充值目标机构（下拉选择）
- 充值申请金额（元）（正数，保留两位小数）
- 收款时间（默认当前）
- 备注

**交互流程：**
1. 选择充值中心 → 填写金额和时间 → 提交
2. 申请状态=待审核

**积分增加比例：**
- >=10万 → 充值金额×2%
- <10万 → 充值金额×1%

---

#### 4.2.4 B端充值审批（总部管理员）

**审批页面展示：**
- 申请人
- 目标充值中心
- 申请金额
- 充值中心上月消耗金额（调用商城接口）
- 适用积分比例
- 本次增加积分
- 申请人备注

**审批操作：**
- **通过：** 充值中心积分余额增加（+本次增加积分）→ 记录操作日志
- **拒绝：** 填写拒绝原因 → 申请状态=已拒绝

---

#### 4.2.5 C端充值录入（充值中心操作员，直接扣余额，无需审批）

**前置条件：** 充值中心积分余额必须 ≥ 充值金额

**页面元素：**
- 用户手机号（必填）
- user_id（必填，与手机号二选一）
- 用户姓名（选填）
- 充值积分金额（最低1000，必须是1000的倍数）
- 收款机构（系统自动）
- 操作人（系统自动）
- 支付方式（银行转账/微信/支付宝/其他）
- 交易流水号（选填）
- 收款时间（默认当前）
- 备注

**交互流程：**
1. 填写用户信息 → 填写充值金额
2. 点击提交
3. 系统检查余额 → 若不足提示，若充足则执行以下步骤：
   - 扣减门店积分余额（数据库乐观锁）
   - 增加商城会员积分（调用商城接口）
   - 记录操作日志
   - 发送积分通知（异步）

**余额不足处理：**
- **前端校验：** 提交前检查余额是否充足，不足时禁用提交按钮并提示
- **后端校验：** 扣减时使用数据库乐观锁防止并发超扣
- **提示文案：** "门店积分余额不足，请联系总公司充值（当前余额：XXX积分）"

**充值失败处理：**
- **阶段1：余额扣减成功，商城接口调用失败** → 自动回滚余额 + 记录失败日志 + 定时任务补偿重试（Q5方案A）
- **阶段2：余额扣减失败** → 前端提示用户重试

**商城接口失败重试机制：**
- **重试策略：** 指数退避（30s / 60s / 120s / 300s）
- **最大重试次数：** 5次
- **重试超时：** 10分钟
- **超时后处理：** 标记为"待人工处理"，通知运营人员手动介入
- **重试期间状态：** 充值记录显示"处理中"，用户可见但不可操作

**积分通知机制：**
- **通知方式：** 异步消息队列（MQ）
- **通知内容：** user_id、充值金额、充值时间、充值中心
- **通知失败：** 自动重试，失败后记录日志供运营查询

---

#### 4.2.6 公司总卡库管理（超管）

**需求描述：** 超管对公司总卡库进行管理，总库用于推荐奖励和购买套餐包的实体卡发放。

**页面元素：**
- 卡号段批量入库
  - 起始卡号
  - 结束卡号
  - 卡数量（系统自动计算）
  - 备注
- 总库库存统计
  - 总卡数
  - 已发放数
  - 剩余库存数
  - 库存告警阈值（可设置，默认100张）
- 库存告警记录
  - 告警时间
  - 告警类型
  - 处理状态

**交互流程：**
1. 超管输入卡号段（如：TJT1000001 - TJT1000100）
2. 系统校验卡号段：
   - 卡号格式正确
   - 卡号未被使用
   - 卡号不重复
3. 批量入库到公司总卡库，卡状态为"已入库"
4. 更新库存统计

**库存告警：**
- 告警阈值：默认100张，可配置
- 触发条件：剩余库存 ≤ 告警阈值
- 告警方式：系统消息 + 后台通知
- 告警频次：每天最多1次（避免打扰）
- 告警对象：超管、总部管理员

**数据表设计：**

```sql
-- 公司总卡库表
CREATE TABLE card_total_inventory (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    card_no VARCHAR(32) UNIQUE NOT NULL COMMENT '卡号',
    card_type TINYINT DEFAULT 1 COMMENT '卡类型:1=实体卡,2=虚拟卡',
    status TINYINT DEFAULT 1 COMMENT '状态:1=已入库,2=已发放,3=已激活,4=已冻结,5=已过期,6=已作废',
    recharge_center_id BIGINT COMMENT '充值中心ID（发放后绑定）',
    user_id BIGINT COMMENT '用户ID（发放后绑定）',
    batch_no VARCHAR(64) COMMENT '批次号',
    issue_reason VARCHAR(64) COMMENT '发放原因:购买套餐包/推荐奖励/其他',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

    KEY idx_status (status),
    KEY idx_recharge_center_id (recharge_center_id),
    KEY idx_user_id (user_id)
);
```

---

#### 4.2.7 运营人员绑定卡号和用户

**需求描述：** 实体卡线下发放后，运营人员在后台输入卡号和用户手机号进行绑定。

**适用场景：**
- 购买套餐包实体卡发放：用户在门店购买套餐包后，运营人员发放实体卡并绑定
- 推荐奖励实体卡发放：用户推荐他人购买后，到门店找运营人员领取实体卡并绑定

**页面元素：**
- 卡号（必填）
- 用户手机号（必填）
- 发放原因（购买套餐包/推荐奖励/其他）
- 关联购买人手机号（推荐奖励时必填）
- 备注

**交互流程：**
1. 运营人员输入卡号 → 系统校验卡号是否在库存中
   - 卡号存在 → 继续下一步
   - 卡号不存在 → 提示"卡号不存在，请先入库"
   - 卡号已发放 → 提示"该卡已发放给用户XXX"
2. 输入用户手机号 → 系统调用商城接口验证用户是否存在
   - 用户存在 → 显示用户信息（姓名、user_id）
   - 用户不存在 → 提示"用户不存在，请确认手机号"
3. 选择发放原因 → 系统校验是否符合发放条件
   - 购买套餐包 → 验证购买记录
   - 推荐奖励 → 验证推荐资格（被推荐人购买满7天、购买数>退款数）
4. 确认绑定 → 系统更新卡状态为"已发放"，绑定到user_id
5. 记录操作日志

**异常处理：**

| 异常场景 | 处理方式 | 提示文案 |
|---------|---------|---------|
| 卡号不存在 | 拦截 | "卡号不存在，请先确认卡号或联系超管入库" |
| 卡号已发放 | 拦截 | "该卡已发放给用户XXX（手机号：138****1234），无法重复发放" |
| 用户不存在 | 拦截 | "用户不存在，请确认手机号是否正确" |
| 不符合发放条件 | 拦截 | 根据具体原因提示（如：购买记录不存在、推荐资格未满足等） |
| 发放原因选择错误 | 拦截 | "发放原因与实际情况不符，请确认后重试" |

**数据表设计：**

```sql
-- 卡发放记录表
CREATE TABLE card_issue_record (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    card_no VARCHAR(32) NOT NULL COMMENT '卡号',
    user_id BIGINT NOT NULL COMMENT '用户ID',
    user_phone VARCHAR(32) NOT NULL COMMENT '用户手机号',
    issue_reason VARCHAR(64) NOT NULL COMMENT '发放原因:购买套餐包/推荐奖励/其他',
    issue_type TINYINT NOT NULL COMMENT '发放方式:1=实体卡（运营人员绑定）,2=虚拟卡（用户自助领取）',
    recharge_center_id BIGINT NOT NULL COMMENT '充值中心ID',
    operator_id BIGINT NOT NULL COMMENT '操作员ID',
    related_user_phone VARCHAR(32) COMMENT '关联购买人手机号（推荐奖励时）',
    remark VARCHAR(500) COMMENT '备注',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,

    KEY idx_card_no (card_no),
    KEY idx_user_id (user_id),
    KEY idx_recharge_center_id (recharge_center_id),
    UNIQUE KEY uk_card_no (card_no)
);
```

---

#### 4.2.8 门店卡开店发放（总库→充值中心，超管操作）

新店开业或库存不足时，超管将公司总卡库中的卡划拨到各充值中心的库存中。卡状态仍为"已入库"，需后续由充值中心管理员/操作员发放给消费者。

**页面元素：**
- 目标充值中心（下拉选择）
- 卡来源（公司总卡库或指定卡号段）
- 卡号段（起始卡号-结束卡号）
- 备注

---

#### 4.2.9 查询用户消费记录

操作员输入用户手机号或user_id，系统调用商城接口查询该用户在商城的消费记录。

**商城返回：**
- 是否购买过套餐包
- 推荐他人购买数量
- 可发放卡数量
- 历史已发放卡数量
- 上月消耗金额（用于计算返点）

**展示逻辑：**
- 若符合条件 → 展示"发放门店卡"按钮
- 若不符合 → 提示不满足发放条件

**说明：** 本功能用于发放前验证，实际发放操作见4.2.7节（运营人员绑定卡号和用户）或4.2.12节（会员门店卡领取）。

---

#### 4.2.10 门店卡消费核销

**页面元素：**
- 充值中心（系统自动）
- 卡号
- 消费金额（元）（正整数，最低100元）
- 消费时间（默认当前）
- 备注

**交互流程：**
1. 输入卡号 → 查询卡信息（余额/状态/持卡人/激活时间/有效期）
2. 判断状态是否为"已发放"且未冻结
3. 判断是否过期（当前日期 > 激活日期 + 1年）
4. 输入消费金额 → 校验金额≤卡余额
5. 扣减余额，记录核销日志

**卡余额校验：**
- **前端校验：** 输入消费金额时，实时提示剩余可用余额
- **后端校验：** 核销时再次校验余额是否充足（防止并发核销）
- **提示文案：** "消费金额超卡余额（当前余额：XXX元）"

**过期检查：**
- **检查时机：** 实时检查（每次核销前）
- **过期规则：** 当前日期 > 激活日期 + 1年
- **过期处理：** ❌ 拦截核销，提示"该卡已过期（有效期至XXXX-XX-XX），无法核销"
- **过期通知：** 核销失败即为通知

**核销失败处理：**
| 失败原因 | 处理方式 | 提示文案 |
|---------|---------|---------|
| 卡不存在 | 拦截 | "卡号不存在，请检查" |
| 卡未发放或已冻结 | 拦截 | "该卡状态异常，无法核销" |
| 卡已过期 | 拦截 | "该卡已过期（有效期至XXXX-XX-XX），无法核销" |
| 消费金额超余额 | 拦截 | "消费金额超卡余额（当前余额：XXX元）" |
| 网络异常 | 允许重试 | "核销失败，请重试" |
| 数据库异常 | 记录日志 | "核销异常，请联系技术支持" |

---

#### 4.2.11 C端小程序（用户查看端）

为充值中心配置微信小程序，供持卡消费者查看自己持有的门店会员卡信息和消费记录。

**小程序功能：**
- 门店卡列表（卡号/余额/状态/有效期）
- 门店卡详情（含消费记录）
- 门店信息（名称/地址/电话）
- 会员门店卡领取（推荐奖励）

**登录方式：** 微信授权获取手机号，登录后根据手机号查询持有的所有门店卡

---

#### 4.2.12 会员门店卡领取（推荐奖励虚拟卡）

**需求描述：** 会员（持卡消费者）在小程序中手动领取推荐奖励门店卡。

**进入路径：** 小程序首页 → "我的卡券" → "待领取门店卡"

**页面元素：**
- 可领取门店卡列表（卡片式展示）
- 每张卡片显示：
  - 被推荐人手机号（脱敏显示）
  - 可领取卡数量
  - 资格生效时间（被推荐人购买满7天时间）
  - 领取按钮

**交互流程：**
1. 用户进入页面 → 系统调用后端接口查询可领取的门店卡
2. 后端验证逻辑：
   - 推荐人必须是会员（持卡消费者）
   - 被推荐人购买套餐包满7天（过退款周期）
   - 被推荐人最近7天购买数 > 退款数
   - 被推荐人与当前推荐人是绑定关系
3. 验证通过 → 展示可领取列表
4. 用户点击"领取"按钮 → 后端发放门店卡 → 卡状态变为"已发放"
5. 领取成功 → 提示"领取成功" → 刷新列表

**后端验证失败处理：**
| 失败原因 | 提示文案 |
|---------|---------|
| 推荐人不是会员 | "仅会员可领取推荐奖励" |
| 被推荐人未过退款周期 | "被推荐人购买未满7天，请稍后再试" |
| 被推荐人购买数≤退款数 | "被推荐人已退款，不符合领取条件" |
| 推荐关系不成立 | "推荐关系异常，请联系客服" |

**领取后有效期：** 从激活日（首次核销）开始计算1年有效期

**可领取资格：** 永久有效，随时可领取

---

## 五、外部系统接口

### 7.1 C端充值通知（本系统 → 商城）

```
POST /api/notify/recharge

参数：
- user_id: 用户ID
- amount: 充值积分金额
- recharge_center_id: 充值中心ID
- transaction_no: 交易流水号
- time: 充值时间
```

### 7.2 用户消费记录查询（本系统 → 商城）

用于：①判断门店卡发放条件 ②计算门店返点

```
GET /api/query/user/consumption

参数：
- user_id 或 phone

商城返回：
- has_purchased_package: 是否购买过套餐包
- referral_count: 推荐他人购买数量
- purchasable_card_count: 可发放卡数量
- already_issued_count: 历史已发放卡数量
- last_month_consumption: 上月消耗金额
```

### 7.3 资产变更回调（商城 → 本系统）

```
POST /api/callback/asset-change

参数：
- event_id: 幂等键
- event_type: 事件类型
- user_id: 用户ID
- user_phone: 用户手机号
- change_amount: 变动金额
- change_type: 变动类型
- recharge_center_id: 充值中心ID
- operator_id: 操作员ID
- transaction_no: 交易流水号
- change_time: 变动时间
- sign: MD5签名校验
```

### 7.4 推荐奖励资格查询（本系统 → 商城）

用于：用户在小程序领取推荐奖励门店卡时，查询可领取的门店卡列表

```
GET /api/query/referral/eligible

参数：
- referrer_user_id: 推荐人user_id（必填）

商城返回：
```json
{
  "referrer_user_id": 370955981,
  "is_member": true,
  "eligible_referrals": [
    {
      "referred_user_id": 370955982,
      "referred_phone": "138****1234",
      "purchase_time": "2026-04-01T10:00:00Z",
      "eligible_time": "2026-04-08T10:00:00Z",
      "purchase_count": 1,
      "refund_count": 0,
      "card_count": 1,
      "is_issued": false
    }
  ]
}
```

**字段说明：**
- `is_member`: 推荐人是否是会员（持卡消费者）
- `eligible_referrals`: 符合领取条件的被推荐人列表
- `eligible_time`: 资格生效时间（购买满7天）
- `purchase_count`: 最近7天购买数
- `refund_count`: 最近7天退款数
- `is_issued`: 是否已领取

### 7.5 门店卡发放通知（本系统 → 商城）

用于：门店卡发放成功后，通知商城系统记录

```
POST /api/notify/card-issue

参数：
```json
{
  "card_no": "TJT0000001",
  "user_id": 370955981,
  "issue_reason": "推荐奖励",
  "issue_time": "2026-04-08T10:00:00Z",
  "recharge_center_id": 1,
  "transaction_no": "TXN20260408100001"
}
```

### 7.6 接口失败重试机制

**重试策略：** 指数退避（30s / 60s / 120s / 300s）

| 接口 | 最大重试次数 | 重试超时 | 超时后处理 |
|------|-------------|---------|-----------|
| C端充值通知（7.1） | 5次 | 10分钟 | 标记"待人工处理"，通知运营 |
| 门店卡发放通知（7.5） | 5次 | 10分钟 | 记录日志，定时任务补偿 |
| 推荐奖励资格查询（7.4） | 3次 | 5分钟 | 前端提示用户重试 |

**重试期间状态：**
- 充值记录显示"处理中"，用户可见但不可操作
- 门店卡发放记录显示"通知中"

**幂等性保证：**
- 所有接口使用`transaction_no`作为幂等键
- 商城接口需支持幂等处理（重复请求返回相同结果）

---

## 六、非功能需求

### 7.1 性能需求

- 页面响应 ≤2秒(P99)
- 导出1000条 ≤10秒
- 接口响应 ≤500ms(P99)
- 支持并发 ≥100用户

### 7.2 安全需求

- 手机号+密码/短信验证码登录
- RBAC权限模型（5级角色）
- 手机号脱敏展示
- 哈希链防篡改
- 回调签名验证
- bcrypt加密密码

### 7.3 兼容性需求

- 浏览器支持Chrome/Edge/Firefox最新两个版本
- 移动端暂不支持PC端以外（小程序除外）

### 7.4 数据备份

- 每日全量+实时增量备份
- 操作日志、充值记录等在线保留2个月
- 2个月后的数据归档存储（后续使用ES）
- **归档数据查询机制：**
  - **查询入口：** 保留"历史记录"菜单入口
  - **查询范围：** 可查询所有归档数据（无时间限制）
  - **查询性能:**
    - 近3个月数据： 直接查询MySQL（快速响应）
    - 3个月以上数据: 查询ES（秒级响应）
  - **数据完整性:** 归档数据只读,不可修改
  - **查询功能:** 支持按时间、操作人、操作类型等维度筛选

---

## 七、异常场景处理 ⭐ v1.1新增

### 7.1 场景1：B端充值申请与审批

#### 7.1.1 Q1：审批拒绝后，申请单如何处理？

**处理方案：驳回修改**

**流程：**

| 步骤 | 操作 | 说明 |
|------|------|------|
| 1 | 审批拒绝 | 审批人必须填写拒绝理由（必填，文本输入） |
| 2 | 申请单状态 | 变更为"已拒绝" |
| 3 | 申请人修改 | 可修改：充值金额/目标充值中心/收款时间/备注 |
| 4 | 重新提交 | 申请单状态变更为"待审核" |
| 5 | 重新审批 | 审批人可查看审批历史，再次通过或拒绝 |

**界面提示：**
- 拒绝时弹窗：必填"拒绝理由"输入框
- 申请人查看：显示拒绝理由、原申请信息，可修改后重新提交
- 审批人审批：显示完整审批历史（原申请金额→拒绝→修改后金额）

---

#### 7.1.2 Q2：审批通过后，充值中心余额更新失败怎么办？

**处理方案：补偿机制 + 定时任务重试**

**流程：**

1. **主流程：**
   - 更新 `b_recharge.status = '已通过'` ✅
   - 尝试更新 `recharge_center.balance += add_points`
     - 成功 → 完成 ✅
     - 失败 → 进入补偿流程 ⚠️

2. **补偿流程（失败时）：**
   - 记录到异常表：`b_recharge_exception`
     - `recharge_id`: 申请单ID
     - `recharge_center_id`: 充值中心ID
     - `add_points`: 应增加积分
     - `retry_count`: 0（初始重试次数）
     - `status`: '待重试'
     - `error_msg`: 失败原因
   - 给审批人返回："审批成功，余额到账中"

3. **定时任务（每5分钟扫描一次）：**
   - 扫描 `b_recharge_exception`，`status = '待重试'`
   - 尝试更新余额
     - 成功 → `status = '已处理'` ✅
     - 失败 → `retry_count + 1` ⚠️
       - `retry_count < 10` 且 `created_at + 1小时 > NOW()` → 继续待重试
       - `retry_count >= 10` 或 `created_at + 1小时 <= NOW()` → `status = '处理失败'` ❌

4. **失败告警（10次重试都失败或超时1小时）：**
   - 标记申请单为"异常"状态
   - 发送告警通知技术团队（邮件/钉钉/飞书）
   - 告警内容：申请单ID、充值中心、失败原因
   - 人工介入处理：
     - 检查数据库连接/表锁定等问题
     - 手动更新余额
     - 标记为'已处理'
     - 记录人工处理日志

**数据表设计：**

```sql
CREATE TABLE b_recharge_exception (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    recharge_id BIGINT NOT NULL COMMENT '充值申请单ID',
    recharge_center_id BIGINT NOT NULL COMMENT '充值中心ID',
    add_points DECIMAL(20,2) NOT NULL COMMENT '应增加积分',
    retry_count INT DEFAULT 0 COMMENT '重试次数',
    status TINYINT DEFAULT 1 COMMENT '状态:1=待重试,2=已处理,3=处理失败',
    error_msg VARCHAR(500) COMMENT '错误信息',
    last_retry_time DATETIME COMMENT '最后重试时间',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_recharge_id (recharge_id)
);
```

---

#### 7.1.3 Q3：审批过程中，充值中心被冻结了怎么办？

**处理方案：区分B端和C端**

| 操作类型 | 充值中心冻结时 | 处理方式 |
|---------|--------------|---------|
| **B端充值审批** | 允许审批 | ✅ 审批通过后自动解冻充值中心<br>✅ 记录"系统自动解冻"操作日志 |
| **C端充值** | 不允许 | ❌ 提示"充值中心已被冻结，如需充值请联系总公司" |

**业务逻辑说明：**
- B端充值是资金流入（总公司→充值中心），即使冻结也应允许
- C端充值是资金流出（充值中心→会员），冻结期间应禁止
- 自动解冻记录操作日志，保留审计轨迹

---

### 7.2 场景2：C端充值录入

#### 7.2.1 Q4：充值中心余额不足时怎么办？

**处理方案：双重检查机制**

**流程：**

**环节1：输入时实时检查（失焦时）**
- 操作员输入充值金额：10,000
- 输入框失焦（鼠标移开或按Tab）
- 前端实时查询当前余额：5,000
- 若余额不足：
  - 输入框下方显示黄色提示：⚠️ "当前余额5,000积分，不足以充值10,000积分"
  - 输入框标红，但允许继续输入

**环节2：提交时最终校验**
- 操作员点击"提交充值"
- 后端再次检查余额（防止并发导致的余额不足）
- 若余额不足：
  - 弹窗提示：❌ "充值失败\n当前余额：5,000积分\n充值金额：10,000积分\n请联系总公司充值"
  - 不允许提交，返回表单

**优点：**
- 实时检查：用户体验好，减少无效操作
- 提交校验：数据安全，避免并发问题

---

#### 7.2.2 Q5：调用商城接口加积分失败了怎么办？

**处理方案：异步重试 + 完整日志 + 手动补偿 + 飞书告警**

**整体流程：**

```
1. C端充值主流程（同步）
   ├─ 检查余额是否充足
   ├─ 扣减充值中心余额 ✅
   ├─ 创建充值记录（状态='待同步'）⚠️
   ├─ 记录操作日志 ✅
   ├─ 发送异步消息到重试队列
   └─ 提示操作员："充值成功，积分将在5分钟内到账"

2. 异步重试流程（后台）
   ├─ 消费者从队列取消息
   ├─ 调用商城接口加积分
   ├─ 成功 → 更新状态='已同步' ✅
   └─ 失败 → 记录失败日志，继续重试 ⚠️
       ├─ 重试策略：指数退避（30s / 60s / 120s / 300s）
       ├─ 最大重试：5次
       └─ 4次后仍失败 → 状态='同步失败' ❌

3. 失败告警与通知
   ├─ 5次重试失败后：
   │   ├─ 发送飞书告警通知技术团队
   │   ├─ 标记充值记录为'同步失败'
   │   └─ 进入手动处理队列
   └─ 通知内容：
       ├─ 充值记录ID
       ├─ 用户信息（手机号/user_id）
       ├─ 充值金额
       ├─ 失败原因
       └─ 重试次数

4. 后台手动处理（管理端新增功能）
   ├─ 菜单：数据管理 → 同步失败记录
   ├─ 列表展示：
   │   ├─ 充值记录ID
   │   ├─ 充值时间
   │   ├─ 充值中心
   │   ├─ 用户手机号/user_id
   │   ├─ 充值金额
   │   ├─ 失败原因
   │   ├─ 重试次数（5/5）
   │   ├─ 最后失败时间
   │   └─ 操作按钮：
   │       ├─ [查看详细日志] 📋
   │       ├─ [手动重试] 🔄
   │       ├─ [标记已处理] ✅
   │       └─ [联系商城] 📞
   └─ 操作：
       ├─ 查看详细日志 → 调用商城接口的请求/响应
       ├─ 手动重试 → 立即调用一次商城接口
       ├─ 标记已处理 → 确认已线下处理，关闭工单
       └─ 联系商城 → 快速联系商城技术支持
```

**数据表设计：**

```sql
-- C端充值同步日志表
CREATE TABLE c_recharge_sync_log (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    recharge_id BIGINT NOT NULL COMMENT '充值记录ID',
    user_id BIGINT NOT NULL COMMENT '会员ID',
    user_phone VARCHAR(32) NOT NULL COMMENT '会员手机号',
    amount DECIMAL(20,2) NOT NULL COMMENT '充值金额',
    retry_count INT DEFAULT 0 COMMENT '重试次数',
    max_retry INT DEFAULT 5 COMMENT '最大重试次数',
    status TINYINT DEFAULT 1 COMMENT '状态:1=待同步,2=同步中,3=已同步,4=同步失败',

    -- 商城接口调用信息
    mall_request_id VARCHAR(64) COMMENT '商城请求ID（幂等键）',
    mall_request_body JSON COMMENT '商城请求体',
    mall_response_body JSON COMMENT '商城响应体',
    mall_error_msg VARCHAR(500) COMMENT '商城错误信息',
    mall_response_code VARCHAR(32) COMMENT '商城响应码',

    -- 时间戳
    first_sync_time DATETIME COMMENT '首次同步时间',
    last_sync_time DATETIME COMMENT '最后同步时间',
    success_time DATETIME COMMENT '同步成功时间',

    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

    UNIQUE KEY uk_recharge_id (recharge_id),
    KEY idx_status (status),
    KEY idx_user_id (user_id),
    KEY idx_created_at (created_at)
);

-- 手动处理记录表
CREATE TABLE c_recharge_manual_handle (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    recharge_id BIGINT NOT NULL COMMENT '充值记录ID',
    handle_type TINYINT NOT NULL COMMENT '处理类型:1=手动重试,2=标记已处理,3=线下补偿',
    handler_id BIGINT NOT NULL COMMENT '处理人ID',
    handle_result TINYINT COMMENT '处理结果:1=成功,2=失败',
    handle_remark VARCHAR(500) COMMENT '处理备注',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,

    KEY idx_recharge_id (recharge_id),
    KEY idx_handler_id (handler_id)
);
```

**配置文件：**

```yaml
# 异步重试配置
mall:
  sync:
    enabled: true
    retry_interval: 60  # 重试间隔（秒）
    max_retry: 5        # 最大重试次数
    queue_size: 1000    # 队列大小
    worker_num: 5       # 消费者数量

# 飞书告警配置
alert:
  enabled: true
  channels:
    - type: feishu
      name: "技术团队告警群"
      webhook: "${FEISHU_CRITICAL_WEBHOOK}"
      at:
        enabled: true
        user_ids: ["ou_xxxxx", "ou_yyyyy"]  # @具体的人
    - type: feishu
      name: "值班人员群"
      webhook: "${FEISHU_DUTY_WEBHOOK}"
      at:
        enabled: true
        all: true  # @所有人
```

**飞书告警消息格式：**

```
┌─────────────────────────────────────────┐
│ 【C端充值同步失败告警】⚠️                 │
│                                          │
│ 充值记录ID: 12345                        │
│ 充值时间: 2026-04-08 14:30               │
│ 充值中心: 北京子公司合伙人               │
│ 用户手机号: 138****1234                  │
│ 充值金额: 1,000.00积分                   │
│ 失败原因: 商城接口超时                   │
│ 重试次数: 5/5                            │
│                                          │
│ [点击查看详情] 📋  [手动重试] 🔄           │
│                                          │
│ @技术团队 @张三 @李四                     │
└─────────────────────────────────────────┘
```

---

#### 7.2.3 Q6：商城系统宕机期间，C端充值能否继续？

**处理方案：后台开关 + 完全停止**

**设计思路：**
- 超管可在后台开启"商城维护模式"
- 开启后，所有依赖商城的功能暂停
- 用户看到友好的维护提示

**开关配置（数据库存储）：**

```sql
CREATE TABLE system_config (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    config_key VARCHAR(64) UNIQUE NOT NULL COMMENT '配置键',
    config_value TEXT COMMENT '配置值',
    config_type VARCHAR(32) DEFAULT 'string' COMMENT '配置类型',
    description VARCHAR(255) COMMENT '配置说明',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 初始化商城维护开关
INSERT INTO system_config (config_key, config_value, config_type, description) VALUES
('mall.maintenance.enabled', 'false', 'boolean', '商城系统维护开关：true=开启维护，false=正常'),
('mall.maintenance.reason', '', 'string', '维护原因'),
('mall.maintenance.estimated_recovery', '', 'datetime', '预计恢复时间');
```

**后台管理界面：**

```
系统管理
  └─ 系统配置
      └─ 商城维护开关

┌─────────────────────────────────────────┐
│  商城系统维护开关                        │
│                                         │
│  当前状态：🟢 正常运行                   │
│                                         │
│  [开启维护模式]                          │
│                                         │
│  操作历史：                              │
│  2026-04-08 15:30  张三  开启维护       │
│    原因：商城系统升级维护                │
│    预计恢复：2026-04-08 18:00            │
│  2026-04-08 18:05  李四  关闭维护       │
└─────────────────────────────────────────┘
```

**C端充值拦截逻辑：**

```go
// 检查商城维护状态
enabled, status, err := config.IsMaintenanceEnabled(db)
if enabled {
    return &MaintenanceError{
        Reason:            status.Reason,
        EstimatedRecovery: status.EstimatedRecovery,
    }
}
```

**用户提示：**

```
┌─────────────────────────────────────────┐
│  系统维护提示                            │
│                                         │
│  ⚠️ 商城系统维护中                       │
│                                         │
│  当前时间：2026-04-08 16:00              │
│  维护原因：商城系统升级维护              │
│  预计恢复：2026-04-08 18:00              │
│                                         │
│  请稍后再试，如有疑问请联系客服           │
│                                         │
│  [关闭] [联系客服]                       │
└─────────────────────────────────────────┘
```

**影响范围：**

| 功能模块 | 是否影响 | 说明 |
|---------|---------|------|
| **C端充值** | ✅ 暂停 | 不允许录入，提示"系统维护中" |
| **查询用户消费记录** | ✅ 暂停 | 调用商城接口，需暂停 |
| **门店卡发放** | ✅ 暂停 | 需查询消费记录判断条件 |
| **B端充值** | ❌ 不影响 | 内部流程，不依赖商城 |
| **门店卡核销** | ❌ 不影响 | 内部流程，不依赖商城 |
| **数据导出** | ❌ 不影响 | 导出本地数据 |

**开启维护时通知（飞书）：**

```
【商城维护模式已开启】⚠️

操作人：张三(admin)
开启时间：2026-04-08 15:30

维护原因：
商城系统升级维护

预计恢复时间：2026-04-08 18:00

影响范围：
- C端充值功能暂停
- 用户消费记录查询暂停

请相关负责人知晓！

@技术团队 @运营团队 @值班人员
```

---

### 7.3 场景3：门店卡核销

#### 7.3.1 Q7：卡核销时余额不足怎么办？

**处理方案：严格拦截**

**流程：**

| 步骤 | 操作 | 说明 |
|------|------|------|
| 1 | 操作员输入卡号 | 查询卡信息（余额/状态/持卡人） |
| 2 | 输入核销金额 | 提交时校验：核销金额 ≤ 卡余额？ |
| 3 | 余额检查 | 后端再次检查余额（防止并发导致的余额不足） |
| 4 | 拦截核销 | 弹窗提示："❌ 核销失败\n当前余额：500.00元\n核销金额：600.00元\n余额不足，请重新输入" |

**前端优化（可选）：**
- 输入核销金额时，失焦后检查：若超过余额，输入框标红
- 提示：⚠️ "核销金额超过卡余额（当前余额500元）"

---

#### 7.3.2 Q8：同一张卡被同时核销两次怎么办？（并发场景）

**处理方案：数据库乐观锁（version字段）**

**并发控制原理：**

```
┌─────────────────────────────────────────────────────────┐
│  数据库乐观锁流程                                       │
│                                                         │
│  1. 查询卡信息（包含version字段）                        │
│     SELECT id, card_no, balance, version                 │
│     FROM card_record                                 │
│     WHERE card_no = 'TJ00012345'                       │
│     → 返回：balance=500, version=3                         │
│                                                         │
│  2. 提交核销时更新（带version条件）                        │
│     UPDATE card_record                                 │
│     SET balance = balance - 200,                         │
│         version = version + 1                            │
│     WHERE card_no = 'TJ00012345' AND version = 3         │
│                                                         │
│  3. 检查影响行数：                                         │
│     ├─ RowsAffected = 1 → 更新成功 ✅                   │
│     └─ RowsAffected = 0 → 版本号已变，更新失败 ❌        │
│        → 提示："该卡正在被其他操作员核销，请稍后重试"        │
└─────────────────────────────────────────────────────────┘
```

**Go实现示例：**

```go
// 核销时使用乐观锁
func (s *CardService) Consume(req *ConsumeRequest) error {
    // 1. 查询卡和版本号
    var card Card
    if err := s.db.Where("card_no = ?", req.CardNo).First(&card).Error; err != nil {
        return err
    }
    
    // 2. 检查余额
    if req.Amount > card.Balance {
        return errors.New("余额不足")
    }
    
    // 3. 乐观锁更新
    result := s.db.Model(&card).
        Where("version = ?", card.Version).
        Updates(map[string]interface{}{
            "balance": card.Balance - req.Amount,
            "version": gorm.Expr("version + 1"),
        })
    
    if result.RowsAffected == 0 {
        return errors.New("该卡正在被其他操作员核销，请稍后重试")
    }
    
    // 4. 记录核销日志
    return nil
}
```

**前端自动重试：**

```javascript
async submitConsume() {
  // 最多重试3次
  for (let i = 0; i < 3; i++) {
    try {
      await api.cardConsume(this.form)
      uni.showToast({ title: '核销成功', icon: 'success' })
      return
    } catch (error) {
      if (error.message.includes('正在被其他操作员核销')) {
        if (i < 2) { // 前两次重试
          uni.showLoading({ title: `处理中...${i + 1}/3` })
          await this.sleep(1000)
          uni.hideLoading()
          continue
        } else { // 第3次失败
          uni.hideLoading()
          uni.showModal({
            title: '核销失败',
            content: '该卡正在被其他操作员核销，请稍后重试',
            showCancel: false
          })
        }
      }
    }
  }
}
```

**优点：**
- ✅ 实现简单，依赖数据库
- ✅ 不会出现死锁
- ✅ 自动失败重试

---

#### 7.3.3 Q9：卡已冻结，但紧急情况需要核销怎么办？

**处理方案：总部管理员解冻 + 线下协商**

**权限设计：**

| 角色 | 默认拥有解冻权限 | 说明 |
|------|-----------------|------|
| **超管** | ✅ 是 | 系统最高权限，可解冻所有卡 |
| **总部管理员** | ✅ 是 | 可解冻所有中心的冻结卡 |
| **充值中心管理员** | ❌ 否 | 不能解冻卡 |
| **充值中心操作员** | ❌ 否 | 不能解冻卡 |

**解冻 + 核销流程：**

```
┌─────────────────────────────────────────────────────────┐
│ 1. 充值中心操作员输入卡号：TJ00012345                    │
│    └─ 查询状态：已冻结 ❌                                │
└─────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────┐
│ 2. 系统提示                                             │
│    "该卡已冻结，无法核销                                  │
│     冻结原因：疑似被盗刷                                  │
│     冻结时间：2026-04-08 10:00                          │
│     冻结人：李四（总部管理员）                            │
│     如需核销，请联系总部管理员解冻"                      │
│                                                         │
│    [关闭]  [联系总部]                                    │
└─────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────┐
│ 3. 操作员点击"联系总部"                                  │
│    ├─ 显示总部管理员列表：                              │
│    │   ├─ 张三（超管）138****0001                      │
│    │   └─ 李四（总部管理员）138****0002                    │
│    ├─ 点击 → 快速拨打电话/发送微信消息                 │
│    └─ 预设消息："您好，门店有卡TJ00012345需要紧急解冻，  │
│                会员王先生现场确认，麻烦处理一下"          │
└─────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────┐
│ 4. 总部管理员收到请求，登录后台                          │
│    ├─ 进入：门店卡管理 → 搜索卡TJ00012345               │
│    ├─ 查看：冻结原因、冻结时间、持卡人信息              │
│    ├─ 点击"解冻"按钮                                    │
│    ├─ 填写解冻原因（必填）：                            │
│    │   "会员王先生现场确认，身份证已核实，同意解冻"       │
│    └─ 提交 → 卡状态变为"已发放" ✅                      │
│                                                         │
│    └─ 系统通知：                                       │
│        ├─ 通知申请的操作员："卡TJ00012345已解冻"         │
│        └─ 记录解冻日志                                  │
└─────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────┐
│ 5. 操作员收到通知，继续核销流程                          │
│    ├─ 重新输入卡号                                      │
│    ├─ 查询状态：正常 ✅                                  │
│    └─ 继续核销...                                       │
└─────────────────────────────────────────────────────────┘
```

**解冻接口权限检查：**

```go
// 权限检查：只有超管和总部管理员可以解冻
if role != 0 && role != 3 { // 0=超管, 3=总部管理员
    return errors.New("无权限解冻，请联系总部管理员")
}
```

---

### 7.4 场景4：门店卡发放

#### 7.4.1 Q10：发放时验证用户存在性

**处理方案：双重验证机制**

**流程：**

| 步骤 | 操作 | 说明 |
|------|------|------|
| **1. 查询消费记录** | 输入 user_id/手机号 → 调用商城接口 | 返回消费记录，判断发卡条件 |
| **2. 展示信息** | 显示查询到的用户信息，准备发卡 | 确认用户身份 |
| **3. 再次验证** | 确认发放时，验证 user_id 是否存在 | ✅ 存在 → 继续发放<br>❌ 不存在 → 拦截发放 |

**拦截逻辑：**

```
调用商城接口：POST /api/mall/user/verify
请求：{ "user_id": 370955981 }

返回：
├─ {"exists": true} ✅ → 继续发放
└─ {"exists": false} ❌ → 拦截发放

拦截后处理：
  1. 不扣减卡库存
  2. 不绑定卡到用户
  3. 弹窗提示：
     "❌ 发放失败
      用户不存在或已被删除
      user_id：370955981
      请重新查询用户"
  4. 操作员点击"确定" → 返回步骤1
```

**Go实现：**

```go
// 发放前验证用户
func (s *CardService) IssueCard(req *IssueCardRequest) error {
    // 1. 先验证用户是否存在
    exists, err := s.mallClient.VerifyUser(req.UserID)
    if err != nil {
        return err
    }
    
    if !exists {
        return &UserNotExistsError{
            UserID: req.UserID,
            Message: "用户不存在或已被删除，请重新查询用户",
        }
    }
    
    // 2. 检查卡库存
    // 3. 扣减卡库存
    // 4. 绑定卡到用户
    // ...
    
    return nil
}
```

---

#### 7.4.2 Q11：发放后发现发错人了怎么办？

**处理方案：冻结卡片 + 线下协商**

**整体思路：** 发现发错 → 冻结卡 → 线下协商 → 解冻或换发新卡

**权限控制：**

| 角色 | 冻结权限 | 说明 |
|------|---------|------|
| **超管** | ✅ 可冻结所有卡 | 系统最高权限 |
| **总部管理员** | ✅ 可冻结所有卡 | 审批权限 |
| **充值中心管理员** | ✅ 可冻结本中心所有卡 | 管理权限 |
| **充值中心操作员** | ✅ 可冻结本中心所有卡 | 管理权限 |

**冻结流程：**

```
┌─────────────────────────────────────────────────────────┐
│ 1. 发现发错，申请冻结                                     │
│                                                         │
│  操作员查看发放历史：                                    │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 发放记录                                       │   │
│  │   卡号：TJ00012345                              │   │
│  │   发放时间：2026-04-08 14:00                    │   │
│  │   绑定用户：王先生 (370955981)                   │   │
│  │   发放原因：购买套餐包                           │   │
│  │   操作员：张三                                   │   │
│  │                                                │   │
│  │ [查看详情]  [冻结卡片] 🆕                      │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  操作员点击"冻结卡片"                                    │
│  ├─ 弹窗："冻结原因（必填）"                            │
│  │   ┌─────────────────────────────────────────┐     │   │
│  │   │ 发放错误，应该是发给李先生，误发给王先生  │     │   │
│  │   └─────────────────────────────────────────┘     │   │
│  │                                                │     │   │
│  │   [取消]  [确认冻结]                              │   │
│  └─ 确认冻结 → 系统提示：                                │   │
│      "冻结后，该卡将无法消费                                │   │
│       请确认是否冻结？"                                   │   │
│      [取消]  [确认冻结]                                  │   │
│                                                         │
│  权限检查：                                            │
│  ├─ 操作员：只能冻结自己发放的卡 ✅                    │
│  └─ 管理员：可以冻结本中心所有卡 ✅                      │
└─────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────┐
│ 2. 冻结完成                                             │
│                                                         │
│  系统执行：                                              │
│  1. 卡状态：已发放 → 已冻结 ✅                           │
│  2. 记录冻结日志：                                      │
│     ├─ 操作员：张三                                     │
│     ├─ 操作：冻结卡片                                   │
│     ├─ 原因：发放错误，应该是发给李先生                     │
│     └─ 时间：2026-04-08 14:05                           │
│  3. 通知绑定的用户（可选）：                            │
│     ├─ 发送消息给王先生                                 │
│     └─ "您的门店卡TJ00012345已被冻结，请联系门店"        │
│                                                         │
│  提示操作员：                                          │
│  "卡TJ00012345已冻结                                    │
│   该卡现在无法消费                                      │
│   请联系持卡人进行线下协商"                              │
└─────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────┐
│ 3. 线下协商                                             │
│                                                         │
│  操作员联系王先生：                                      │
│  "您好，我们发现误将门店卡发放给您了                        │
│   卡号：TJ00012345                                      │
│   该卡已被冻结，无法消费                                  │
│   请您到门店处理"                                        │
│                                                         │
│  情况A：王先生配合 ✅                                    │
│  └─ 退回误发的卡 → 系统解冻 → 卡状态回到"已入库"        │
│     → 重新发放给正确的李先生                             │
│                                                         │
│  情况B：王先生不配合 ❌                                    │
│  └─ 卡保持冻结状态，无法消费                              │
│     → 损失由发卡人（操作员/门店）承担                   │
│     → 如有申诉，走线下申诉流程                            │
└─────────────────────────────────────────────────────────┘
```

**申诉记录表：**

```sql
CREATE TABLE card_appeal (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    card_no VARCHAR(32) NOT NULL COMMENT '卡号',
    appeal_user_id BIGINT COMMENT '申诉人ID（持卡人）',
    appeal_user_name VARCHAR(64) COMMENT '申诉人姓名',
    appeal_user_phone VARCHAR(32) COMMENT '申诉人电话',
    appeal_reason VARCHAR(500) NOT NULL COMMENT '申诉原因',
    appeal_time DATETIME NOT NULL COMMENT '申诉时间',
    status TINYINT DEFAULT 1 COMMENT '状态:1=待处理,2=处理中,3=已解决,4=已拒绝',
    handler_id BIGINT COMMENT '处理人ID',
    handle_result VARCHAR(500) COMMENT '处理结果',
    handle_time DATETIME COMMENT '处理时间',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    
    KEY idx_card_no (card_no),
    KEY idx_status (status),
    KEY idx_appeal_time (appeal_time)
);
```

---

### 7.5 场景5：基础设施异常

#### 7.5.1 Q12：MySQL数据库连接失败

**处理方案：连接池优化 + 熔断机制 + 降级策略**

**系统配置：**

| 配置项 | 当前值 | 优化建议 | 说明 |
|--------|--------|---------|------|
| **数据库版本** | MySQL 5.7 | ✅ 保持 | 稳定版本 |
| **最大连接数** | 100 | 建议 200 | 避免连接耗尽 |
| **连接超时** | 10秒 | 建议 5秒 | 快速失败，避免等待 |
| **连接池** | 是 | ✅ 保持 | 使用连接池 |

**连接池配置：**

```go
// 连接池配置
sqlDB.SetMaxOpenConns(150)              // 最大打开连接数
sqlDB.SetMaxIdleConns(20)               // 最大空闲连接数
sqlDB.SetConnMaxLifetime(30 * time.Minute)  // 连接最大生命周期
sqlDB.SetConnMaxIdleTime(5 * time.Minute)   // 空闲连接超时
```

**熔断机制：**

```
┌─────────────────────────────────────────────────────────┐
│  熔断器模式：关闭 → 开启 → 半开                         │
│                                                         │
│  关闭：正常访问数据库                                     │
│  └─ 连续失败5次 → 开启熔断                              │
│                                                         │
│  开启：快速失败，不访问数据库                             │
│  └─ 30秒后 → 进入半开状态                               │
│                                                         │
│  半开：允许1个请求通过检测                               │
│  ├─ 成功 → 关闭熔断                                    │
│  └─ 失败 → 重新开启熔断                                  │
└─────────────────────────────────────────────────────────┘
```

**降级策略：**

| 业务类型 | 降级策略 | 说明 |
|---------|---------|------|
| **P0 - 核心业务** | 重试3次，间隔2秒 | B端充值审批、C端充值、门店卡核销 |
| **P1 - 重要业务** | 重试1次，快速失败 | 查询充值记录、查询卡信息 |
| **P2 - 一般业务** | 不重试，快速失败 | 操作日志查询、统计报表 |
| **P3 - 后台任务** | 等待连接恢复 | 异步任务、定时任务 |

**用户提示：**

```
┌─────────────────────────────────────────┐
│ ⚠️ 系统繁忙                              │
│                                          │
│ 当前访问人数较多，请稍后重试            │
│ 预计恢复时间：1分钟                     │
│                                          │
│ 您可以：                                  │
│  - 稍后自动刷新                           │
│  - 返回上一页                               │
│                                          │
│ [返回]  [刷新]                              │
└─────────────────────────────────────────┘
```

---

#### 7.5.2 Q13：Redis宕机（未来优化）

**当前方案：数据库乐观锁**

- 使用数据库 `version` 字段实现并发控制
- 适合中小流量，性能满足需求
- 实现简单，依赖少

**后期优化（流量增大后）：Redis分布式锁**

```
Redis分布式锁优势：
├─ 性能更好，减少数据库压力
├─ 支持分布式部署
└─ 锁超时自动释放

实施方案：
├─ 配置Redis 7.2 + 哨兵模式
├─ 锁获取超时：10秒
└─ 锁超时自动释放
```

---

#### 7.5.3 Q14：Excel文件导出失败

**处理方案：超时控制 + 重试机制 + 友好提示**

**场景分析：**
- 文件类型：Excel（.xlsx）
- 数据量：<1000条（小文件）
- 生成耗时：<10秒
- 失败场景：数据库查询慢、网络中断、权限错误等

**导出流程：**

```
┌─────────────────────────────────────────────────────────┐
│ 1. 用户点击"导出Excel"                                  │
│    ├─ 选择导出类型：B端充值记录                         │
│    ├─ 选择时间范围：2026-04-01 ~ 2026-04-08                │
│    └─ 点击"导出" → 显示"正在导出..."                       │
└─────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────┐
│ 2A. 导出成功                                            │
│    ┌─────────────────────────────────────────────────┐   │
│    │ ✅ 导出成功                                     │   │
│    │                                                │   │
│    │ 文件名：B端充值记录_20260401-20260408.xlsx      │   │
│    │ 记录数：856条                                   │   │
│    │ 生成时间：3秒                                   │   │
│    │                                                │   │
│    │ [下载文件]  [关闭]                             │   │
│    └─────────────────────────────────────────────────┘   │
│                                                         │
│    点击"下载文件" → 触发浏览器下载                       │
└─────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────┐
│ 2B. 导出失败                                            │
│    ┌─────────────────────────────────────────────────┐   │
│    │ ❌ 导出失败                                     │   │
│    │                                                │   │
│    │ 失败原因：数据库查询超时                        │   │
│    │                                                │   │
│    │ 您可以：                                        │   │
│    │  - 缩小时间范围后重试                           │   │
│    │  - 减少筛选条件后重试                           │   │
│    │  - 联系技术支持                                 │   │
│    │                                                │   │
│    │ [重新导出]  [联系技术支持]                     │   │
│    └─────────────────────────────────────────────────┘   │
│                                                         │
│  失败日志已记录，技术团队会收到告警通知                 │
└─────────────────────────────────────────────────────────┘
```

**超时控制：**

- 数据库查询：30秒超时
- Excel生成：10秒超时
- 文件上传：重试3次，间隔递增

**错误分类与提示：**

| 错误码 | 错误信息 | 建议 |
|--------|---------|------|
| QUERY_TIMEOUT | 数据查询超时 | 缩小时间范围或减少筛选条件 |
| EXCEL_GENERATE_FAILED | Excel生成失败 | 请稍后重试 |
| UPLOAD_FAILED | 文件上传失败 | 检查网络连接 |
| PERMISSION_DENIED | 无导出权限 | 联系管理员开通权限 |

**导出记录表：**

```sql
CREATE TABLE export_log (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL COMMENT '导出人ID',
    export_type VARCHAR(32) NOT NULL COMMENT '导出类型',
    file_name VARCHAR(255) COMMENT '文件名',
    record_count INT COMMENT '记录数',
    file_size BIGINT COMMENT '文件大小（字节）',
    status TINYINT DEFAULT 1 COMMENT '状态:1=成功,2=失败',
    error_code VARCHAR(64) COMMENT '错误码',
    error_msg VARCHAR(500) COMMENT '错误信息',
    duration_ms INT COMMENT '耗时（毫秒）',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    
    KEY idx_user_id (user_id),
    KEY idx_status (status),
    KEY idx_created_at (created_at)
);
```

---

### 7.6 场景6：推荐奖励门店卡领取

#### 7.6.1 Q15：推荐奖励领取资格校验失败怎么办？

**处理方案：清晰提示 + 分层错误处理**

**校验失败场景分类：**

| 失败场景 | 提示文案 | 后续操作 |
|---------|---------|---------|
| **推荐人不是会员** | "仅会员可领取推荐奖励，您当前不是会员" | 引导用户成为会员 |
| **被推荐人未过退款周期** | "被推荐人购买未满7天，请于2026-04-15后再试" | 显示可领取时间 |
| **被推荐人购买数≤退款数** | "被推荐人已退款，不符合领取条件" | - |
| **推荐关系不成立** | "推荐关系异常，请联系客服处理" | 提供客服联系方式 |
| **商城接口超时** | "系统繁忙，请稍后重试" | 自动重试3次 |

**分层错误处理：**

```
┌─────────────────────────────────────────────────────────┐
│ 1. 用户进入"待领取门店卡"页面                            │
│    └─ 调用后端接口查询可领取列表                         │
└─────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────┐
│ 2A. 接口调用成功 ✅                                      │
│    └─ 展示可领取门店卡列表                              │
└─────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────┐
│ 2B. 接口调用失败 ❌                                      │
│    └─ 根据错误类型显示不同提示                           │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ 错误类型A：推荐人不是会员                                │
│ ┌─────────────────────────────────────────────────┐   │
│ │ ❌ 暂无推荐奖励可领取                           │   │
│ │                                                │   │
│ │ 推荐奖励仅限会员领取                            │   │
│ │ 您当前还不是会员                                │   │
│ │                                                │   │
│ │ [了解会员权益]  [成为会员]                     │   │
│ └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ 错误类型B：被推荐人未过退款周期                          │
│ ┌─────────────────────────────────────────────────┐   │
│ │ ⏳ 暂未满足领取条件                             │   │
│ │                                                │   │
│ │ 被推荐人购买未满7天                             │   │
│ │ 可领取时间：2026-04-15 10:00                    │   │
│ │                                                │   │
│ │ [知道了]  [设置提醒]                           │   │
│ └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ 错误类型C：商城接口超时                                  │
│ ┌─────────────────────────────────────────────────┐   │
│ │ ⚠️ 系统繁忙                                    │   │
│ │                                                │   │
│ │ 查询失败，请稍后重试                            │   │
│ │                                                │   │
│ │ [重新加载]  [联系客服]                         │   │
│ └─────────────────────────────────────────────────┘   │
│                                                         │
│ 自动重试逻辑：                                          │
│ ├─ 前端自动重试3次，间隔递增（1s/2s/3s）                │
│ └─ 3次仍失败 → 显示上述提示                            │
└─────────────────────────────────────────────────────────┘
```

**前端自动重试实现：**

```javascript
async function fetchEligibleReferrals() {
  const maxRetries = 3;
  const delays = [1000, 2000, 3000]; // 1s, 2s, 3s

  for (let i = 0; i < maxRetries; i++) {
    try {
      const response = await api.getEligibleReferrals();
      return response.data;
    } catch (error) {
      if (error.code === 'MALL_TIMEOUT' && i < maxRetries - 1) {
        await sleep(delays[i]);
        continue;
      }
      throw error;
    }
  }
}
```

---

#### 7.6.2 Q16：领取门店卡时发放失败怎么办？

**处理方案：事务回滚 + 状态记录 + 人工介入**

**发放失败处理流程：**

```
┌─────────────────────────────────────────────────────────┐
│ 1. 用户点击"领取"按钮                                   │
│    └─ 调用后端接口发放门店卡                            │
└─────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────┐
│ 2A. 发放成功 ✅                                         │
│    ├─ 卡状态：未发放 → 已发放                          │
│    ├─ 卡绑定：绑定到推荐人user_id                      │
│    ├─ 记录发放日志                                      │
│    └─ 前端提示："领取成功"                              │
└─────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────┐
│ 2B. 发放失败 ❌                                         │
│    └─ 根据失败原因分类处理                              │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ 失败类型A：卡库存不足                                    │
│ ┌─────────────────────────────────────────────────┐   │
│ │ ❌ 领取失败                                     │   │
│ │                                                │   │
│ │ 门店卡库存不足，暂时无法领取                    │   │
│ │ 请稍后重试或联系门店                            │   │
│ │                                                │   │
│ │ [重试]  [联系门店]                             │   │
│ └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ 失败类型B：数据库异常                                    │
│ ┌─────────────────────────────────────────────────┐   │
│ │ ❌ 领取失败                                     │   │
│ │                                                │   │
│ │ 系统异常，领取失败                              │   │
│ │ 我们已记录该问题，请稍后重试                    │   │
│ │                                                │   │
│ │ [重试]  [联系客服]                             │   │
│ └─────────────────────────────────────────────────┘   │
│                                                         │
│ 后端处理：                                              │
│ ├─ 记录失败日志到 `card_issue_exception` 表            │
│ ├─ 发送告警通知技术团队                                │
│ └─ 定时任务每5分钟扫描一次，自动重试                  │
└─────────────────────────────────────────────────────────┘
```

**数据表设计：**

```sql
-- 门店卡发放异常表
CREATE TABLE card_issue_exception (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    referrer_user_id BIGINT NOT NULL COMMENT '推荐人user_id',
    referred_user_id BIGINT NOT NULL COMMENT '被推荐人user_id',
    card_count INT NOT NULL COMMENT '应发放卡数量',
    retry_count INT DEFAULT 0 COMMENT '重试次数',
    status TINYINT DEFAULT 1 COMMENT '状态:1=待重试,2=已发放,3=发放失败',
    error_msg VARCHAR(500) COMMENT '错误信息',
    last_retry_time DATETIME COMMENT '最后重试时间',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

    UNIQUE KEY uk_referrer_referred (referrer_user_id, referred_user_id),
    KEY idx_status (status)
);
```

---

### 7.7 场景7：C端小程序登录

#### 7.7.1 Q17：微信授权获取手机号失败怎么办？

**处理方案：降级登录方案 + 友好提示**

**微信授权失败场景：**

| 失败场景 | 提示文案 | 降级方案 |
|---------|---------|---------|
| **用户拒绝授权** | "需要授权手机号才能查看门店卡" | 提供"重新授权"按钮 |
| **微信接口超时** | "微信接口繁忙，请稍后重试" | 自动重试3次 |
| **非微信环境** | "请在微信中打开小程序" | 引导用户在微信中打开 |
| **用户未绑定手机号** | "请先绑定手机号" | 引导用户绑定手机号 |

**降级登录流程：**

```
┌─────────────────────────────────────────────────────────┐
│ 1. 用户打开小程序                                        │
│    └─ 触发微信授权获取手机号                            │
└─────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────┐
│ 2A. 授权成功 ✅                                         │
│    ├─ 获取手机号                                        │
│    ├─ 调用后端接口查询用户门店卡                        │
│    └─ 进入首页                                          │
└─────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────┐
│ 2B. 授权失败 ❌                                         │
│    └─ 根据失败类型显示不同提示                           │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ 场景A：用户拒绝授权                                      │
│ ┌─────────────────────────────────────────────────┐   │
│ │ ⚠️ 需要授权手机号                              │   │
│ │                                                │   │
│ │ 查看门店卡需要授权手机号                        │   │
│ │ 请点击下方按钮重新授权                          │   │
│ │                                                │   │
│ │ [重新授权]  [取消]                             │   │
│ └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ 场景B：微信接口超时                                      │
│ ┌─────────────────────────────────────────────────┐   │
│ │ ⚠️ 微信接口繁忙                                │   │
│ │                                                │   │
│ │ 授权失败，请稍后重试                            │   │
│ │                                                │   │
│ │ [重试]  [稍后再试]                             │   │
│ └─────────────────────────────────────────────────┘   │
│                                                         │
│ 自动重试逻辑：                                          │
│ ├─ 前端自动重试3次，间隔递增（1s/2s/3s）                │
│ └─ 3次仍失败 → 显示上述提示                            │
└─────────────────────────────────────────────────────────┘
```

**前端重试实现：**

```javascript
async function getPhoneNumber(e) {
  const maxRetries = 3;
  const delays = [1000, 2000, 3000];

  for (let i = 0; i < maxRetries; i++) {
    try {
      const { code } = e.detail;
      const response = await api.loginByWechat({ code });
      return response.data;
    } catch (error) {
      if (i < maxRetries - 1) {
        await sleep(delays[i]);
        continue;
      }
      // 显示错误提示
      uni.showModal({
        title: '授权失败',
        content: '微信接口繁忙，请稍后重试',
        confirmText: '重试',
        cancelText: '取消'
      });
    }
  }
}
```

---

### 7.8 场景8：门店卡激活与有效期

#### 7.8.1 Q18：门店卡首次核销（激活）失败怎么办？

**处理方案：事务回滚 + 错误日志 + 手动补偿**

**首次核销（激活）失败处理流程：**

```
┌─────────────────────────────────────────────────────────┐
│ 1. 用户首次使用门店卡消费                                │
│    └─ 门店卡激活（首次核销）                            │
└─────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────┐
│ 2A. 激活成功 ✅                                         │
│    ├─ 记录激活时间（首次核销时间）                      │
│    ├─ 计算有效期（激活时间 + 1年）                      │
│    ├─ 扣减卡余额                                        │
│    └─ 记录核销日志                                      │
└─────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────┐
│ 2B. 激活失败 ❌                                         │
│    └─ 根据失败原因分类处理                              │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ 失败类型A：卡余额不足                                    │
│ ┌─────────────────────────────────────────────────┐   │
│ │ ❌ 激活失败                                     │   │
│ │                                                │   │
│ │ 卡余额不足，无法激活                            │   │
│ │ 当前余额：0元                                   │   │
│ │                                                │   │
│ │ [知道了]                                       │   │
│ └─────────────────────────────────────────────────┘   │
│                                                         │
│ 注意：激活失败不会扣除余额，卡状态保持"已发放"          │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ 失败类型B：数据库异常                                    │
│ ┌─────────────────────────────────────────────────┐   │
│ │ ❌ 激活失败                                     │   │
│ │                                                │   │
│ │ 系统异常，激活失败                              │   │
│ │ 请稍后重试或联系门店                            │   │
│ │                                                │   │
│ │ [重试]  [联系门店]                             │   │
│ └─────────────────────────────────────────────────┘   │
│                                                         │
│ 后端处理：                                              │
│ ├─ 事务回滚，不扣除余额                                │
│ ├─ 记录失败日志                                        │
│ └─ 发送告警通知技术团队                                │
└─────────────────────────────────────────────────────────┘
```

**激活事务处理：**

```go
func (s *CardService) ActivateAndConsume(req *ConsumeRequest) error {
    return s.db.Transaction(func(tx *gorm.DB) error {
        // 1. 查询卡信息（带锁）
        var card Card
        if err := tx.Clauses(clause.Locking{Strength: "UPDATE"}).
            Where("card_no = ?", req.CardNo).
            First(&card).Error; err != nil {
            return err
        }

        // 2. 检查余额
        if req.Amount > card.Balance {
            return errors.New("余额不足")
        }

        // 3. 首次核销，记录激活时间
        if card.ActivatedAt == nil {
            now := time.Now()
            card.ActivatedAt = &now
            card.ExpiredAt = now.AddDate(1, 0, 0) // 1年后过期
        }

        // 4. 扣减余额
        card.Balance -= req.Amount

        // 5. 保存卡信息
        if err := tx.Save(&card).Error; err != nil {
            return err
        }

        // 6. 记录核销日志
        log := &ConsumeLog{
            CardNo: req.CardNo,
            Amount: req.Amount,
            OperatorID: req.OperatorID,
        }
        if err := tx.Create(log).Error; err != nil {
            return err
        }

        return nil
    })
}
```

---

## 变更记录

| 版本 | 日期 | 变更内容 | 作者 |
|------|------|----------|------|
| v1.0 | 2026-04-07 | 初稿，基于详细功能设计 v1.7 | 产品团队 |
| v1.1 | 2026-04-08 | 新增异常场景处理规范（Q1-Q6）<br>- 场景1：B端充值（Q1-Q3）<br>- 场景2：C端充值（Q4-Q6） | 产品团队 |
| v1.2 | 2026-04-08 | 新增三、业务规则章节<br>- 积分比例规则、门店卡发放规则<br>- 余额计算与对账规则、核销与售后规则<br>- 完善异常场景处理（场景3-5，Q7-Q14） | 产品团队 |
| v1.3 | 2026-04-08 | 完善功能需求章节（4.2.5/4.2.8/4.2.9/4.2.10/4.2.11）<br>- 补充C端充值录入的余额校验、失败重试、积分通知机制<br>- 补充门店卡发放的购买/推荐奖励差异处理<br>- 补充门店卡核销的过期检查和核销失败处理<br>- 补充C端小程序的会员门店卡领取页面<br>- 完善外部系统接口（7.4/7.5/7.6）<br>- 完善异常场景处理（场景6-8，Q15-Q18） | 产品团队 |

## 八、风险评估

| 风险类型 | 风险描述 | 影响程度 | 应对措施 |
|---------|---------|---------|---------|
| **技术风险** | 哈希链性能开销大 | 低 | 异步写入，不阻塞主事务 |
| **技术风险** | 回调接口被恶意攻击 | 中 | 签名验证 + IP白名单 |
| **业务风险** | 充值中心余额计算错误 | 高 | 日志记录余额快照，支持对账 |
| **业务风险** | 门店卡被重复核销 | 高 | 幂等校验 + 卡状态机管控 |
| **业务风险** | C端充值后商城加积分失败 | 中 | 重试机制（最多5次）+ 异步补偿 |
| **数据风险** | 日志数据被篡改 | 高 | 哈希链 + 禁止删除权限 |
| **合规风险** | 充值记录不可篡改 | 高 | 日志哈希链 + RBAC管控 |
| **运维风险** | 商城系统宕机 | 中 | 维护开关 + 降级策略 |

---

## 九、项目计划

### 10.1 里程碑

| 阶段 | 交付物 | 预计周期 | 负责人 |
|------|--------|---------|--------|
| **需求评审** | PRD定稿 | 第1周 | 产品 |
| **产品设计** | 交互设计稿 + UI设计稿 | 第2-4周 | 设计 |
| **开发阶段（第一批）** | 充值中心管理 + 门店管理 + B端充值（含审批）+ 操作日志 + 登录 | 第5-8周 | 开发 |
| **开发阶段（第二批）** | C端充值（直接扣余额）+ 门店卡管理（入库/发放/核销）+ 导出 | 第9-12周 | 开发 |
| **开发阶段（第三批）** | C端小程序 + 商城接口对接 + 异常处理机制 | 第13-15周 | 开发 |
| **测试阶段** | 功能测试 + 安全测试 + 性能测试 | 第16-18周 | 测试 |
| **验收上线** | UAT验收 + 生产部署 | 第19周 | 产品+运维 |

### 10.2 依赖项

| 依赖方 | 内容 | 备注 |
|--------|------|------|
| **商城** | 提供C端充值通知接口 + 消费记录查询接口 + 资产变更回调接口 | 需商城侧配合 |
| **运维** | K3s集群环境准备 | 生产部署前置 |
| **设计** | PC Web端交互和UI设计 + 微信小程序设计与开发 | 并行 |
| **短信服务** | 短信验证码发送 | 需接入短信API |

---

## 十、附录

### 10.1 术语表

| 术语 | 解释 |
|------|------|
| **B端充值** | 总公司财务运营为充值中心申请充值（需审批），审批通过后充值中心积分余额增加 |
| **C端充值** | 充值中心操作员直接为商城会员充值积分，扣本店余额，无需审批 |
| **充值中心** | 与门店1:1绑定的管理单元，有独立积分账户 |
| **余额** | 充值中心当前可用积分 = 累计公司打入积分 - 已给会员充值积分 |
| **积分比例** | B端充值审批时，根据充值中心上月商城消费金额确定（>=10万=2%，<10万=1%） |
| **门店卡** | 预付费卡，面值1000元/张，用于门店消费核销 |
| **user_id** | 跨系统唯一标识，来自商城，用于识别同一会员 |
| **哈希链** | 每条日志含上一条日志的哈希值，防止历史日志被篡改 |
| **佣金** | 全部线下结算，本系统不涉及 |

### 10.2 参考文档

- 太积堂充值与门店管理系统 - 详细功能设计 v1.7
- 太积堂充值与门店管理系统 - 开发实现文档 v1.1
- 商城系统接口文档

### 10.3 变更记录

| 版本 | 日期 | 变更内容 | 作者 |
|------|------|----------|------|
| v1.0 | 2026-04-07 | 初稿，基于详细功能设计 v1.7 | 产品团队 |
| v1.1 | 2026-04-08 | 新增异常场景处理规范（六、异常场景处理）<br>- 场景1：B端充值（Q1-Q3）<br>- 场景2：C端充值（Q4-Q6）<br>- 场景3-5：待补充<br>- 补充商城维护开关配置<br>- 补充飞书告警配置 | 产品团队 |
| v1.2 | 2026-04-08 | 新增三、业务规则章节<br>- 积分比例规则、门店卡发放规则<br>- 余额计算与对账规则、核销与售后规则<br>- 完善异常场景处理（Q7-Q14） | 产品团队 |
| v1.3 | 2026-04-08 | 完善功能需求章节<br>- 补充C端充值录入的余额校验、失败重试、积分通知机制<br>- 补充门店卡发放的购买/推荐奖励差异处理<br>- 补充门店卡核销的过期检查和核销失败处理<br>- 补充C端小程序的会员门店卡领取页面<br>- 完善外部系统接口（推荐奖励查询、门店卡发放通知、重试机制）<br>- 完善异常场景处理（场景6-8，Q15-Q18）<br>- 修正文档冲突点（月末→月初对账、小程序登录方式） | 产品团队 |
